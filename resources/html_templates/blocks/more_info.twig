<table class="more-info">
    <tbody>
        {% for taxId in invoice.taxIds %}
            <tr>
                <th>{{ taxId.label }}:</th>
                <td>{{ taxId.value | nl2br }}</td>
            </tr>
        {% endfor %}
        {% if invoice.paymentTerms != null %}
            <tr>
                <th>{{ string('payment_terms') }}:</th>
                <td>{%  include 'fields/payment_terms.twig' %}</td>
            </tr>
        {% endif %}
    </tbody>
</table>
